#include <REGX52.H>
#include "D:\Th.VDK\BAI6\Thu_Vien_LCD.h"
int dem=0;
	void main()
		{
		LCD_Init();// khoi tao LCD
			LCD_Gotoxy(0,0); //x la cot, y la hang
			LCD_PutNumber(111); // hien thi nhieu so tren LCD
			LCD_Gotoxy(0,1); //x la cot, y la hang
			LCD_PutNumber(777); // hien thi nhieu so tren LCD
			while(1)
				{}
					}
